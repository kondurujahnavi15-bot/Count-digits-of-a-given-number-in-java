# Count-digits-of-a-given-number-in-java
a simple Java program that calculates the total number of digits present in a given integer. The program accepts an integer input from the user and uses an iterative approach to count how many digits the number contains.
